name: CI/CD counter app

on:
  push:
    branches: [ Master ]

jobs:

  Deploy:
    # needs: build
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2 

      - name: Create key Pair
        run: |
          aws ec2 create-key-pair --key-name AWS_PRIVATE_KEY
      - name: Deploy in EC2
        env:
            # PRIVATE_KEY: ${{ secrets.AWS_PRIVATE_KEY  }}
            HOSTNAME : ${{ secrets.HOSTNAME  }}
            USER_NAME : ${{ secrets.USER_NAME  }}
            
        run: |

          ls
          echo "--------------"
          pwd

          sudo chmod 400 private_key
          echo "-------ssh-------"
          sudo ssh -o StrictHostKeyChecking=no -i private_key ${USER_NAME}@${HOSTNAME} 
          echo "------acp--------" 
          scp -o StrictHostKeyChecking=no -i private_key.pem -r DummyFolder ${USER_NAME}@${{secrets.AWS_DNS}}:/home/ubuntu/test/
          # echo "$PRIVATE_KEY" > private_key 
          # cat private_key


